import React, { Component } from 'react'
import { Input as ReactStrapInput } from 'reactstrap'

export default class Input extends Component {
    render() {
        return (
            <ReactStrapInput style={{ fontFamily: }} />
        )
    }
}
